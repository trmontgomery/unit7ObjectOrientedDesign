import java.awt.Graphics2D;
import java.awt.Rectangle;
import java.awt.geom.Ellipse2D;
import java.awt.geom.Line2D;
import java.awt.geom.Point2D;
import java.awt.Color;

public class Triangle
{
    
    
    public Triangle()
    {
    }
    
    public void draw(Graphics2D g2)
    {
        
    }
}

/*
/**
 *public class Building
{
    /** ideas: can you randomize the postion
       can you make sliding doors? 
       should there be a separate window class? */
   // private int xLeft;
    //private int yTop;
    //private int height;
    //private int width;

    /*
     *   public Building(int x, int y, int h, int w)
    {
        The coordinates 0,0 would place the Target at the top Left corner
        xLeft = x;
        yTop = y;
        height = h;
        width = w;
    }
     */
  

    /*
      public void draw(Graphics2D g2)
    {
        g2.setColor(Color.blue);
        g2.drawRect(xLeft, yTop, width, height);
        g2.fillRect(xLeft, yTop, width, height);
        
        g2.setColor(Color.white);
        g2.fillRect(xLeft+10, yTop+10, 40, 40);
        g2.fillRect(xLeft+40+10+5, yTop+10, 40, 40);
        
        g2.fillRect(xLeft+10, yTop+10+50, 40, 40);
        g2.fillRect(xLeft+40+10+5, yTop+10+50, 40, 40);
        
        g2.fillRect(xLeft+10, yTop+10+50+50, 40, 40);
        g2.fillRect(xLeft+40+10+5, yTop+10+50+50, 40, 40);
        
        
    }
     */
   
    /*
     *
     *
    
    public void addWindows(Graphics2D g2)
    {
        g2.fillRect(xLeft+10, yTop+10+150, 40, 40);
        g2.fillRect(xLeft+40+10+5, yTop+10+150, 40, 40);
        
        g2.fillRect(xLeft+10, yTop+10+200, 40, 40);
        g2.fillRect(xLeft+40+10+5, yTop+10+200, 40, 40);
    }
}
 */
